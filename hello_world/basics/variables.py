print("Hello World 😁")
print("*" * 10)  # this is an expression
# to format your code, use (command + shift + p) and type "format document"
# we've also added a shortcut (command r) to run the code in the terminal

student_count = 1000  # defining a variable
rating = 4.99  # a float variable
is_published = False # a boolean variable (T/F), must start with a capital letter
course_name = "Python Programming"  # a string variable
# variables must be descriptive, lowercase, and use underscores
